using UnityEngine;


public class SpecialObjectProperties : MonoBehaviour
{
    public float lightRadius = 5f;
    public float lightIntensity = 1f;
    public bool canOverbright = false; // 元の色より明るくできるか
    public Color lightColor = Color.white; // 光源の色
}
